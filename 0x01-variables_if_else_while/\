#include <stdio>
/**
 * main -  a program prints the alphabet in lowercase except q and e
 * Return: Always return 0 (success)
 **/
int main(void)
{
	int n;
	
	for (n = 'a'; n <= 'z'; n++)
	{
		if (n != 'e' && n != 'q')
			putchar(n);
	}
	putchar('\n');
	return (0);
}
